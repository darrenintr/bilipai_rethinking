export interface Metric {
  label: string;
  paladalaValue: string;
  officialValue: string;
  improvement: string;
  unit: string;
}

export interface FeatureCard {
  title: string;
  description: string;
  badge: string;
  iconName: string;
}
